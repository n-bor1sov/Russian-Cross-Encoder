# ─────────────────────────────────────────────────────────────────────────────
# HuggingFace cache
# Set these so all scripts store models and datasets in one place.
# ─────────────────────────────────────────────────────────────────────────────
HF_HOME=/path/to/hf_cache
HF_DATASETS_CACHE=/path/to/hf_cache/datasets

# ─────────────────────────────────────────────────────────────────────────────
# Embedding API (used by pipeline/02_embed/embed_shards.py and pipeline/04_hard_negative_mining/mine.py)
# Expects an OpenAI-compatible embeddings endpoint.
# ─────────────────────────────────────────────────────────────────────────────
EMBEDDING_API_BASE_URL=https://your-embedding-api.example.com/v1
EMBEDDING_API_KEY=your-api-key-here
EMBEDDING_MODEL_NAME=your-embedding-model-name

# Path to local tokenizer directory for the embedding model (used for truncation)
QWEN_MODEL_PATH=/path/to/qwen-tokenizer

# ─────────────────────────────────────────────────────────────────────────────
# Dataset compilation paths (pipeline/05_compile/compile.py)
# ─────────────────────────────────────────────────────────────────────────────
FILTERING_DATA_ROOT=/path/to/consistency_filtering_data
HNM_ROOT=/path/to/hnm/mined_negatives

# ─────────────────────────────────────────────────────────────────────────────
# Training paths (pipeline/06_train/train.py)
# ─────────────────────────────────────────────────────────────────────────────
TRAIN_MODEL_PATH=/path/to/base/model
TRAIN_DATASET_PATH=/path/to/bucketed/train/dataset
EVAL_DATASET_PATH=/path/to/bucketed/eval/dataset
TRAIN_OUTPUT_DIR=/path/to/training/output
TRAIN_LOG_DIR=./logs

# ─────────────────────────────────────────────────────────────────────────────
# ClearML experiment tracking (optional — set USE_CLEARML=0 to disable)
# ─────────────────────────────────────────────────────────────────────────────
CLEARML_PROJECT_NAME=CrossEncoders
CLEARML_TASK_NAME=reranker-training
USE_CLEARML=1
